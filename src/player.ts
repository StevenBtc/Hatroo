class player extends class entity {
    inventory : string;
    health: number;
    armor: number;

    /*
    * Constructor for objects of class player
    */

    constructor() {
        // initialise instance variables
        this.inventory = inventory;
        this.health = health;
        this.armor = armor;
    }

    getName() : string {
        return name;
    }

    getHealth() : number {
        return currentHealth;
    }

    setArmor() : number {
     // dmg - getDmgReduc 
    }

}