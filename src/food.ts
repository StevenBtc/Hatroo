class food extends class item{
    isUsed: boolean;
    healthRestore: number;

    /*
    * Constructor for objects of class npc
    */

    constructor() {
        // initialise instance variables
        this.healthRestore = 5;
    }

    eatFood() : boolean {
      //  if isUsed = true
            //restore health to player
        }
    }

